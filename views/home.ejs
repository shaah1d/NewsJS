<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title> <%= query %> - NewsJS

    </title>
    <style>
       /* styles.css */
       .recipe-item {
    margin-bottom: 10px; /* Add some space between each recipe item */
    width: calc(33.33% - 20px); /* Set width to 1/3 of the container width */
    float: left; /* Float items to the left */
    margin-right: 20px; /* Add some space between items */
    border: 2px solid black;
    padding: 10px; /* Add padding to the container */
    /* border-radius: 10px; */
    box-sizing: border-box; /* Include padding and border in the total width */
}

.recipe-item img {
    width: 100%; /* Make the image fill the container width */
    height: auto; /* Maintain aspect ratio */
    max-height: calc(16rem - 20px); /* Set maximum height to fit inside the container */
    margin-bottom: 2px; /* Add some space below the image */
}

/* Clear the float after every third item */
.recipe-item:nth-child(3n+3) {
    margin-right: 0; /* Remove margin on the third item of each row */
}

/* Clear the float after every row */
.recipe-item:nth-child(3n+4) {
    clear: both; /* Move to the next line */
}

        a {
            text-decoration: underline;
            color: black;
            font-size: 18px;
            font-weight: 600;
            display: block;
        }
        body {
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        }
        ul {
            list-style: none;
            padding-left: 0;
        }
       p {
        color: black;
            font-size: 16px;
            font-weight: 600;
            margin-bottom: 2px;
       }
       .source {
        text-decoration: none;
        color: black;
        display: block;
       }
     
    </style>
</head>
<body>
    <form action="/search" method="GET">
        <input type="text" name="query" placeholder="Search"> 
        <button type="submit" class="text-center btn btn-primary">Search</button>
    </form>
  
     <div class="container">
        <ul>
            <% for(let i=0; i<titles.length; i++) { %>
            <div class="recipe-item">
                <img src="<%= images[i] %>" alt="">
                <a class="source" href="http://<%= sources[i] %>"><%= sources[i] %></a>
                <a href=<%= urls[i] %>> <%= titles[i] %> </a>
            </div>
            <% } %>
        </ul>
     </div>

</body>

</html>