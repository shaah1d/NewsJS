<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> <%= query %> - NewsJS </title>
    <style>
 
body {
    font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    margin: 0;
    padding: 0;
}
ul {
    list-style: none;
    padding-left: 0;
}

header {
    background-color: #dc3545;
    color: white;
    padding: 20px;
    text-align: center;
    font-size: 24px;
    font-weight: bold;
}


form {
    display: flex;
    justify-content: center;
    margin-bottom: 20px;
    margin-top: 20px;
}
input[type="text"] {
    padding: 8px;
    border: 2px solid #ccc;
    border-radius: 5px;
    font-size: 16px;
    width: 300px;
    margin-right: 10px;
    margin-top: 5px;
}
button {
    padding: 8px 16px;
    background-color: #dc3545;
    border: none;
    border-radius: 5px;
    color: white;
    font-size: 16px;
    cursor: pointer;
    margin-top: 5px;
}
button:hover {
    background-color: #dc3545;
}

.recipe-item {
    margin-bottom: 10px;
    width: calc(33.33% - 20px);
    float: left;
    margin-right: 20px;
    border: 2px solid black;
    padding: 10px;
    box-sizing: border-box;
    transition: 4ms ease-in-out;
}
.recipe-item img {
    width: 100%;
    height: 200px;
    object-fit: cover;
    border-bottom: 1px solid #ddd;
}
.recipe-item:nth-child(3n+3) {
    margin-right: 0;
}
.recipe-item:nth-child(3n+4) {
    clear: both;
}
a {
    color: black;
    font-size: 18px;
    font-weight: 600;
    display: block;
}
.source {
    text-decoration: none;
    color: black;
    display: block;
}

.recipe-item:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
}

.source:hover {
    color: #555;
}

    </style>
</head>
<body>
    <header>NewsJS</header>
    <form action="/search" method="GET">
        <input type="text" name="query" placeholder="Search" autocomplete="off"> 
        <button type="submit">Search</button>
    </form>
  
    <div class="container">
        <ul>
            <% for(let i=0; i<titles.length; i++) { %>
            <div class="recipe-item">
                <img src="<%= images[i] %>" alt="">
                <a class="source" href="http://<%= sources[i] %>"><%= sources[i] %></a>
                <a href="<%= urls[i] %>"><%= titles[i] %></a>
            </div>
            <% } %>
        </ul>
    </div>

</body>
</html>



